info:
  name: get_all_organizations
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiUrl}}/platform-admin/organizations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
