info:
  name: get_all_roles
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiUrl}}/org-admin/roles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
