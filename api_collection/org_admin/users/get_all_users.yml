info:
  name: get_all_users
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiUrl}}/org-admin/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
