info:
  name: get_all_teams
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiUrl}}/org-admin/teams"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
