info:
  name: create_new_team
  type: http
  seq: 3

http:
  method: POST
  url: "{{apiUrl}}/org-admin/teams/"
  body:
    type: json
    data: |-
      {
          "name": "North Kerala"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
