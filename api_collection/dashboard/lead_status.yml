info:
  name: lead_status
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiUrl}}/dashboard/lead-status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
