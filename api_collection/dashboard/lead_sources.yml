info:
  name: lead_sources
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiUrl}}/dashboard/lead-sources"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
